package by.antimad.graduation_project.my_package.controller;

/*import by.antimad.graduation_project.my_package.entity.UserEntity;
import by.antimad.graduation_project.my_package.service.UserService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@CrossOrigin(origins = "http://localhost:3000")
public class MyUserController {
    @Autowired
    private UserService userService;

    //@ResponseBody
    @GetMapping("/users")
    public ResponseEntity<?> someMethod() {

        /*Iterable<UserEntity> userEntities = userService.someMethod();

        String html = "";
        for (UserEntity user : userEntities) {
            html += user.getLastName() + "<br>";
        }
        return html;
        List<UserEntity> userEntityList = userService.someMethod();

        return ResponseEntity.ok(userEntityList);
    }
}*/
